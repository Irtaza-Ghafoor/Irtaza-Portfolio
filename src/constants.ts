// Bump this version whenever the resume PDF content changes,
// so browsers (especially mobile) don't serve a stale cached copy.
export const RESUME_VERSION = "4";
export const RESUME_URL = `/Irtaza-Ahmad-Resume.pdf?v=${RESUME_VERSION}`;
